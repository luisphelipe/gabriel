import { Login } from "./screens";

function App() {
  return (
    <main
      style={{
        height: "100%",
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
      }}
    >
      <Login />
    </main>
  );
}

export default App;
